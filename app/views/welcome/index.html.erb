<div class="wrapper wrapper-padding clearfix">
  <div class="col-1 about home-page-content">
    <h2>About</h2>
    <hr>
    <p>Hi, I'm a software developer living in Toronto, Canada.</p>
    <p>
      I mostly work with Ruby(Rails) and Go. You can find more of my
      projects on <a href="https://github.com/rliu054">GitHub</a>.
    </p>
  </div>

  <div class="col-2 home-page-content">
    <h2>Stuff I've written</h2>
    <hr>

    <% @posts.each do |post| %>
      <h3><a href=""><%= link_to post.title, post %></a></h3>
      <p class="date"><%= post.created_at.strftime("%b %d, %Y\t%I:%M%p") %></p>
    <% end %>

  </div>

  <div class="col-3 home-page-content">
    <h2>Projects I've built</h2>
    <hr>
    <% @projects.each do |project| %>
      <h3><a href=""><%= link_to project.title, project %></a></h3>
      <p class="date"><%= project.created_at.strftime("%b %d, %Y\t%I:%M%p") %></p>
    <% end %>
  </div>
</div>
